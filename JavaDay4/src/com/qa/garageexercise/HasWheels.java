package com.qa.garageexercise;

public interface HasWheels { // interfaces are used to share functionality but not methods. 
	// a good interface should only have one method - why? its just something in OOP. Could have 50 interfaces each with one method!!
	
	public void changeWheels(); // this is an abstract method so has no body  
	}

// can implement the method from inside your